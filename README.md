# FastAPI evaluation

Checking how FastAPI works.

## Demo

Run the live server.

```shell
uvicorn demo.main:app --reload
```

## Tutorial

Run the live server.

```shell
uvicorn tutorial.main:app --reload
```